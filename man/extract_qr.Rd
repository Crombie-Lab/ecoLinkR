% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_qr.R
\name{extract_qr}
\alias{extract_qr}
\title{Extract QR code text and coordinates from images}
\usage{
extract_qr(img_dir, output_dir, pattern = "\\\\.png$", recursive = FALSE)
}
\arguments{
\item{img_dir}{Character. Directory containing input images (e.g.
"data/processed/photos/isolation").}

\item{output_dir}{Character. Directory where per-image CSV files will be
written. The directory will be created if it does not exist.}

\item{pattern}{Character. Regular expression for image file extensions.
Defaults to "\\\\.png$".}

\item{recursive}{Logical. Whether to search subdirectories recursively
(default: \code{FALSE}).}
}
\value{
Invisibly returns a data frame summarizing which files had QR codes
  detected, the decoded text, and the CSV paths (if written).
}
\description{
Scans PNG (or other) images in a directory for QR codes using \pkg{opencv},
and for each image with a detected QR code, writes a CSV file containing
the QR polygon coordinates and decoded text.
}
\details{
Filenames are normalized to a base identifier by stripping directory
and extension (e.g., "C-0003.png" -> "C-0003"), then converted to
lowercase and with dashes removed (e.g., "c0003") for the CSV basename.
}
\examples{
\dontrun{
extract_qr(
  img_dir = "data/processed/photos/isolation",
  output_dir = "data/processed/extracted_qr_text/isolation"
)
}

}
